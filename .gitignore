# Datos con información personal (DNI/NIE en idrh) — NO versionar (RGPD)
/data/
# Salidas generadas (workbooks XLSX de recarga y auditoría, informe HTML local)
/salidas/
*.xlsx
!tests/fixtures/**/*.xlsx

# Bundles de entrega (git bundle) — artefactos de transporte, no fuente
*.bundle

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.mypy_cache/
.coverage
